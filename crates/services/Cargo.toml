[package]
name = "services"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "服务层（Application Service Layer），组合 storage，实现业务用例"

[dependencies]
domain = { path = "../domain" }
storage = { path = "../storage" }
toolkit = { path = "../toolkit" }
registry = { path = "../registry" }
toml = "0.8"
dirs = "5.0"
