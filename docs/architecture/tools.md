# 工具函数模块架构文档

## 📋 概述

本文档是 Workflow CLI 工具函数模块的**总览和快速索引**。由于模块重组，大部分工具函数已迁移到独立的模块目录，每个模块都有详细的架构文档。

**重要说明**：
- **模块重组**：`lib/base/util/` 模块已重组，大部分功能已迁移到独立的模块目录
- **向后兼容**：`base/util` 模块仍然存在，通过重新导出保持向后兼容
- **详细文档**：每个工具模块都有独立的架构文档，请参考相应的文档获取详细信息

---

## 📚 工具函数模块索引

### 核心工具模块

| 模块 | 文档 | 功能描述 |
|------|------|----------|
| **文件系统操作** | [fs.md](./fs.md) | 文件读写、路径处理、目录操作 |
| **系统交互** | [system.md](./system.md) | 平台检测、浏览器操作、剪贴板操作 |
| **解压工具** | [zip.md](./zip.md) | tar.gz 和 zip 文件解压 |
| **校验和工具** | [checksum.md](./checksum.md) | SHA256 哈希计算和验证 |
| **格式化工具** | [format.md](./format.md) | 显示格式化、消息格式化、日期时间格式化 |
| **日志系统** | [logger.md](./logger.md) | 日志级别管理、带颜色的日志输出 |
| **交互式对话框** | [dialog.md](./dialog.md) | 确认、输入、选择、多选、表单对话框 |
| **进度指示器** | [indicator.md](./indicator.md) | 旋转指示器、进度条 |
| **表格输出** | [table.md](./table.md) | 表格构建器和样式 |
| **常量管理** | [constants.md](./constants.md) | 统一管理错误消息、Git 常量、网络常量等 |
| **MCP 配置** | [mcp.md](./mcp.md) | MCP 配置文件读写和管理 |

### 其他基础设施模块

| 模块 | 文档 | 功能描述 |
|------|------|----------|
| **HTTP 客户端** | [http.md](./http.md) | HTTP 请求、响应处理、重试机制 |
| **并发执行器** | [concurrent.md](./concurrent.md) | 并发任务执行 |
| **别名管理** | [alias.md](./alias.md) | 命令别名管理 |

---

## 📁 模块重组说明

### 当前状态

`lib/base/util/` 模块现在主要作为**向后兼容的包装器**，通过重新导出保持向后兼容性。

**当前模块结构**：
```
src/lib/base/util/
├── mod.rs         # 向后兼容包装器，重新导出所有公共 API
└── data/          # 数据处理子模块（已迁移，仅保留空模块）
    └── mod.rs
```

### 已迁移的模块

所有工具函数已迁移到独立的模块目录，每个模块都有详细的架构文档：

- **`lib/base/system/`** → [System 模块架构文档](./system.md)
- **`lib/base/fs/`** → [FS 模块架构文档](./fs.md)
- **`lib/base/zip/`** → [Zip 模块架构文档](./zip.md)
- **`lib/base/checksum/`** → [Checksum 模块架构文档](./checksum.md)
- **`lib/base/format/`** → [Format 模块架构文档](./format.md)
- **`lib/base/logger/`** → [Logger 模块架构文档](./logger.md)
- **`lib/base/dialog/`** → [Dialog 模块架构文档](./dialog.md)
- **`lib/base/indicator/`** → [Indicator 模块架构文档](./indicator.md)
- **`lib/base/table/`** → [Table 模块架构文档](./table.md)
- **`lib/base/constants/`** → [Constants 模块架构文档](./constants.md)
- **`lib/base/mcp/`** → [MCP 模块架构文档](./mcp.md)

---

## 🔄 向后兼容性

**重要说明**：`lib/base/util/mod.rs` 通过重新导出保持向后兼容，原有的导入路径仍然可用。这意味着现有代码无需修改即可继续工作。

**向后兼容的导入路径**：

```rust
// 以下导入路径仍然有效（向后兼容）
use workflow::base::util::file::FileReader;
use workflow::base::util::path::PathAccess;
use workflow::base::util::directory::DirectoryWalker;
use workflow::base::util::browser::Browser;
use workflow::base::util::clipboard::Clipboard;
use workflow::base::util::platform::Platform;
use workflow::base::util::unzip::Unzip;
use workflow::base::util::checksum::Checksum;
use workflow::base::util::date::format_filename_timestamp;
use workflow::base::util::string::mask_sensitive_value;
```

**推荐使用新的导入路径**（更清晰、更符合模块组织）：

```rust
// 推荐使用新的导入路径
use workflow::base::fs::{FileReader, PathAccess, DirectoryWalker};
use workflow::base::system::{Browser, Clipboard, Platform};
use workflow::base::zip::Unzip;
use workflow::base::checksum::Checksum;
use workflow::base::format::date::format_filename_timestamp;
use workflow::base::format::sensitive::mask_sensitive_value;
```

**迁移建议**：
- ✅ **新代码**：使用新的导入路径（更清晰、更符合模块组织）
- ✅ **现有代码**：可以继续使用旧的导入路径（向后兼容）
- ✅ **重构时**：逐步迁移到新的导入路径

---

## 🔄 模块集成

工具函数模块被整个项目广泛使用，每个模块都有独立的架构文档：

- **日志系统**：所有模块使用 `log_*!` 宏输出日志 → [Logger 模块架构文档](./logger.md)
- **剪贴板**：PR、Log 和 Jira 命令使用 `Clipboard::copy()` → [System 模块架构文档](./system.md)
- **浏览器**：PR 命令使用 `Browser::open()` → [System 模块架构文档](./system.md)
- **文件操作**：Lifecycle 命令使用文件操作 → [FS 模块架构文档](./fs.md)
- **解压工具**：Lifecycle 命令使用 `Unzip::extract()` → [Zip 模块架构文档](./zip.md)
- **校验和**：Lifecycle 命令使用 `Checksum::verify()` → [Checksum 模块架构文档](./checksum.md)
- **用户确认**：多个命令使用 `confirm()` 函数 → [Dialog 模块架构文档](./dialog.md)
- **格式化工具**：Lifecycle 命令使用格式化工具 → [Format 模块架构文档](./format.md)
- **平台检测**：Lifecycle 命令使用平台检测 → [System 模块架构文档](./system.md)
- **表格输出**：PR 命令使用 `TableBuilder` → [Table 模块架构文档](./table.md)

---

## 📋 快速参考

### 常用工具函数快速索引

| 功能 | 模块 | 文档 | 主要 API |
|------|------|------|----------|
| **文件读写** | `base::fs` | [fs.md](./fs.md) | `FileReader`, `FileWriter` |
| **路径操作** | `base::fs` | [fs.md](./fs.md) | `PathAccess` |
| **目录遍历** | `base::fs` | [fs.md](./fs.md) | `DirectoryWalker` |
| **平台检测** | `base::system` | [system.md](./system.md) | `Platform` |
| **浏览器操作** | `base::system` | [system.md](./system.md) | `Browser` |
| **剪贴板操作** | `base::system` | [system.md](./system.md) | `Clipboard` |
| **解压文件** | `base::zip` | [zip.md](./zip.md) | `Unzip` |
| **校验和验证** | `base::checksum` | [checksum.md](./checksum.md) | `Checksum` |
| **显示格式化** | `base::format` | [format.md](./format.md) | `DisplayFormatter` |
| **消息格式化** | `base::format` | [format.md](./format.md) | `MessageFormatter` |
| **日志输出** | `base::logger` | [logger.md](./logger.md) | `Logger`, `log_*!` 宏 |
| **交互式对话框** | `base::dialog` | [dialog.md](./dialog.md) | `ConfirmDialog`, `InputDialog`, `SelectDialog` |
| **进度指示器** | `base::indicator` | [indicator.md](./indicator.md) | `Spinner`, `Progress` |
| **表格输出** | `base::table` | [table.md](./table.md) | `TableBuilder`, `TableStyle` |
| **常量管理** | `base::constants` | [constants.md](./constants.md) | 错误消息、Git 常量等 |

---

## ✅ 总结

工具函数模块为整个项目提供通用的基础设施支持。由于模块重组，大部分功能已迁移到独立的模块目录，每个模块都有详细的架构文档。

**设计优势**：
- ✅ **模块化**：功能按领域分类，代码组织清晰
- ✅ **向后兼容**：通过重新导出保持向后兼容
- ✅ **详细文档**：每个模块都有独立的架构文档
- ✅ **易于维护**：模块职责单一，易于维护和扩展

**当前状态**：
- ✅ 所有工具函数已迁移到独立模块
- ✅ `base/util` 模块作为向后兼容包装器保留
- ✅ 每个模块都有详细的架构文档
- ✅ 向后兼容性完整保持

**建议**：
- 📖 **查看详细功能**：请参考相应的模块架构文档
- 🔄 **迁移代码**：新代码使用新的导入路径，现有代码可以逐步迁移
- 📚 **了解架构**：每个模块的详细架构请参考对应的架构文档

---

**最后更新**: 2025-12-27
