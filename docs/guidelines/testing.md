# 测试规范

## 核心规则

| 类型 | 位置 |
|------|------|
| 单元测试 | 源码同文件 `#[cfg(test)]` |
| 集成测试 | 各 crate 的 `tests/` |
| 性能基准 | `crates/*/benches/` |

- **编写**：AAA 模式（Arrange → Act → Assert）
- **命名**：`test_*`，描述清晰
- **独立性**：不依赖其他测试，避免共享可变状态

---

**最后更新**: 2025-02-20
