---
description: 文档生成规则。关于何时以及如何生成文档的规则。
type: always
---

# 文档生成规则

## 文档原则

**简洁优先**：文档只记录核心业务规则；技术实现细节由代码和系统架构决定。

## 核心原则

**文档保持简洁**：只记录核心业务规则；技术实现细节由代码和系统架构决定。

**默认禁止生成文档**：除非用户使用明确的关键词请求（"生成文档"、"创建文档"、"生成报告"、"保存为文档"等），否则一律不生成 `.md` 文档文件。

**关键词优先级规则**：
- **白名单关键词**（"生成文档"、"创建文档"、"生成报告"、"保存为文档"等）优先级最高，出现时允许生成文档
- **黑名单关键词**（"分析"、"总结"、"检查"、"诊断"、"查看"、"审查"等）默认只提供聊天文本响应，禁止生成文档
- **例外规则**：当白名单关键词与黑名单关键词同时出现时，白名单优先级更高，允许生成文档（例如："生成分析文档" → 允许生成，存放到 `docs/analysis/`）

## 文档分类与存储

**核心原则**：根据文档类型自动归类到对应目录；无法确定类型时，默认存放到 `docs/analysis/`。

### 文档分类表

| 文档类型 | 目录 | 命名模式 |
|---------|------|---------|
| 架构设计文档 | `docs/` | `architecture.md`（kebab-case） |
| 开发文档 | `docs/` | `development.md`（kebab-case） |
| 测试文档 | `docs/` | `testing.md`（kebab-case） |
| 版本迁移文档 | `docs/migration/` | `{VERSION}-to-{VERSION}.md` |
| 分析文档 | `docs/analysis/` | `{topic}.md` |

### 文档命名规范

- **参考文档**：使用 `kebab-case.md`（如 `architecture.md`、`development.md`、`testing.md`）
- **临时文档**：不做要求，可随意命名

## 文档索引规则

- **禁止索引**：`docs/requirements/`、`docs/analysis/` 目录下的文档不索引到 `docs/README.md`
- **参考文档**：架构文档、开发文档、测试文档、迁移文档应索引到 `docs/README.md`

## 文档修改规则

禁止在没有用户明确同意的情况下修改现有文档文件。

---

**最后更新**: 2025-02-20
