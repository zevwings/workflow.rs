---
description: 开发规范与指南，包含代码风格、错误处理、测试规范、Git 工作流和代码审查
type: always
---

# 开发规范与指南

## 项目架构

三层架构：
- **CLI 入口层** (`bin/`, `main.rs`): 命令行参数解析和命令分发
- **命令封装层** (`commands/`): CLI 命令封装，处理用户交互
- **核心业务逻辑层** (`lib/`): 所有业务逻辑实现

详细架构参考：`docs/guidelines/architecture.md`

## 核心原则

**默认禁止生成或修改代码**：除非用户使用明确的关键词请求（"生成代码"、"实现"、"修改代码"、"添加功能"、"修复bug"等），否则一律不生成或修改代码文件。

**黑名单关键词**：包含"分析"、"总结"、"检查"、"讨论"、"审查"等词的请求，只能提供聊天文本响应，禁止生成/修改代码。

**详细规范**：所有开发规范请参考 `docs/guidelines/development.md`

## 快速参考

- **代码格式化**：`cargo fmt`（提交前必须运行）
- **代码检查**：`cargo clippy -- -D warnings`（所有警告必须修复）
- **命名约定**：模块/函数/变量 `snake_case`，类型/Trait `PascalCase`，常量 `SCREAMING_SNAKE_CASE`
- **导入顺序**：标准库 → 第三方库 → 项目内部
- **错误处理**：使用 `anyhow::Result<T>` 和 `Context` 添加上下文
- **文档注释**：所有公共 API 必须有 `///` 文档注释，包含参数、返回值、错误、示例

## 代码生成规范

生成代码时必须遵循：

- **代码风格**：使用 `cargo fmt` 格式化，通过 `cargo clippy -- -D warnings` 检查
- **错误处理**：使用 `anyhow::Result<T>`，使用 `Context` 添加上下文
- **文档注释**：为新功能添加完整的文档注释（参数、返回值、错误、示例）
- **模块组织**：遵循三层架构，正确组织模块和依赖关系
- **测试**：为新功能添加单元测试和集成测试，测试放在 `#[cfg(test)]` 模块中或 `tests/` 目录
- **提交规范**：遵循 Conventional Commits 格式（`<type>(<scope>): <subject>`）

## 文档编写规范

- **模板选择**：参考文档使用 `kebab-case.md`（如 `architecture.md`、`development.md`、`testing.md`）
- **时间戳**：文档末尾添加 `**最后更新**: YYYY-MM-DD`（格式：`---` 分隔线后，英文冒号，ISO 8601 日期）

## 开发流程

### 添加新功能

1. 在 `lib/` 中实现核心业务逻辑
2. 在 `commands/` 中添加 CLI 命令封装
3. 在 `main.rs` 中注册命令
4. 添加测试用例
5. 更新文档

### Git 工作流

- **分支策略**：`feature/*`、`fix/*`、`hotfix/*`
- **提交格式**：Conventional Commits（`feat`、`fix`、`docs`、`style`、`refactor`、`test`、`chore`、`perf`、`ci`）
- **提交信息**：主题行不超过 50 个字符，使用祈使语气

### 测试规范

- 单元测试：`#[cfg(test)]` 模块中
- 集成测试：`tests/` 目录
- 目标覆盖率：> 80%，关键业务逻辑：> 90%

### 代码审查

提交 PR 前检查：
- [ ] `cargo fmt`
- [ ] `cargo clippy -- -D warnings`
- [ ] `cargo test` 通过
- [ ] 文档注释完整
- [ ] 错误处理规范
- [ ] 提交信息符合规范

## 注意事项

1. **跨平台支持**：支持 macOS、Linux、Windows
2. **配置文件**：`~/.workflow/config/workflow.toml`（macOS/Linux）或 `%APPDATA%\workflow\config\workflow.toml`（Windows）
3. **错误处理**：使用 `anyhow::Context` 添加上下文
4. **日志**：使用 `tracing` 进行日志记录
5. **Rust 版本**：要求 Rust 1.70 或更高版本

---

**最后更新**: 2025-01-02
